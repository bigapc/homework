# SafeConnect App

SafeConnect is a simple browser-based safety companion with:

- Trusted contact management (saved in localStorage)
- A countdown check-in timer
- A generated SOS message helper

## Run locally

Open `index.html` directly in a browser, or use a static server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
